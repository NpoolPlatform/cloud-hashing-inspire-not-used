// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/cloud-hashing-inspire/pkg/db/ent/predicate"
	"github.com/NpoolPlatform/cloud-hashing-inspire/pkg/db/ent/userspecialreduction"
	"github.com/google/uuid"
)

// UserSpecialReductionQuery is the builder for querying UserSpecialReduction entities.
type UserSpecialReductionQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.UserSpecialReduction
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserSpecialReductionQuery builder.
func (usrq *UserSpecialReductionQuery) Where(ps ...predicate.UserSpecialReduction) *UserSpecialReductionQuery {
	usrq.predicates = append(usrq.predicates, ps...)
	return usrq
}

// Limit adds a limit step to the query.
func (usrq *UserSpecialReductionQuery) Limit(limit int) *UserSpecialReductionQuery {
	usrq.limit = &limit
	return usrq
}

// Offset adds an offset step to the query.
func (usrq *UserSpecialReductionQuery) Offset(offset int) *UserSpecialReductionQuery {
	usrq.offset = &offset
	return usrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (usrq *UserSpecialReductionQuery) Unique(unique bool) *UserSpecialReductionQuery {
	usrq.unique = &unique
	return usrq
}

// Order adds an order step to the query.
func (usrq *UserSpecialReductionQuery) Order(o ...OrderFunc) *UserSpecialReductionQuery {
	usrq.order = append(usrq.order, o...)
	return usrq
}

// First returns the first UserSpecialReduction entity from the query.
// Returns a *NotFoundError when no UserSpecialReduction was found.
func (usrq *UserSpecialReductionQuery) First(ctx context.Context) (*UserSpecialReduction, error) {
	nodes, err := usrq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userspecialreduction.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (usrq *UserSpecialReductionQuery) FirstX(ctx context.Context) *UserSpecialReduction {
	node, err := usrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserSpecialReduction ID from the query.
// Returns a *NotFoundError when no UserSpecialReduction ID was found.
func (usrq *UserSpecialReductionQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = usrq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userspecialreduction.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (usrq *UserSpecialReductionQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := usrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserSpecialReduction entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserSpecialReduction entity is found.
// Returns a *NotFoundError when no UserSpecialReduction entities are found.
func (usrq *UserSpecialReductionQuery) Only(ctx context.Context) (*UserSpecialReduction, error) {
	nodes, err := usrq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userspecialreduction.Label}
	default:
		return nil, &NotSingularError{userspecialreduction.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (usrq *UserSpecialReductionQuery) OnlyX(ctx context.Context) *UserSpecialReduction {
	node, err := usrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserSpecialReduction ID in the query.
// Returns a *NotSingularError when more than one UserSpecialReduction ID is found.
// Returns a *NotFoundError when no entities are found.
func (usrq *UserSpecialReductionQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = usrq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userspecialreduction.Label}
	default:
		err = &NotSingularError{userspecialreduction.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (usrq *UserSpecialReductionQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := usrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserSpecialReductions.
func (usrq *UserSpecialReductionQuery) All(ctx context.Context) ([]*UserSpecialReduction, error) {
	if err := usrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return usrq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (usrq *UserSpecialReductionQuery) AllX(ctx context.Context) []*UserSpecialReduction {
	nodes, err := usrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserSpecialReduction IDs.
func (usrq *UserSpecialReductionQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	if err := usrq.Select(userspecialreduction.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (usrq *UserSpecialReductionQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := usrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (usrq *UserSpecialReductionQuery) Count(ctx context.Context) (int, error) {
	if err := usrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return usrq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (usrq *UserSpecialReductionQuery) CountX(ctx context.Context) int {
	count, err := usrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (usrq *UserSpecialReductionQuery) Exist(ctx context.Context) (bool, error) {
	if err := usrq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return usrq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (usrq *UserSpecialReductionQuery) ExistX(ctx context.Context) bool {
	exist, err := usrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserSpecialReductionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (usrq *UserSpecialReductionQuery) Clone() *UserSpecialReductionQuery {
	if usrq == nil {
		return nil
	}
	return &UserSpecialReductionQuery{
		config:     usrq.config,
		limit:      usrq.limit,
		offset:     usrq.offset,
		order:      append([]OrderFunc{}, usrq.order...),
		predicates: append([]predicate.UserSpecialReduction{}, usrq.predicates...),
		// clone intermediate query.
		sql:    usrq.sql.Clone(),
		path:   usrq.path,
		unique: usrq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		AppID uuid.UUID `json:"app_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserSpecialReduction.Query().
//		GroupBy(userspecialreduction.FieldAppID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (usrq *UserSpecialReductionQuery) GroupBy(field string, fields ...string) *UserSpecialReductionGroupBy {
	grbuild := &UserSpecialReductionGroupBy{config: usrq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := usrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return usrq.sqlQuery(ctx), nil
	}
	grbuild.label = userspecialreduction.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		AppID uuid.UUID `json:"app_id,omitempty"`
//	}
//
//	client.UserSpecialReduction.Query().
//		Select(userspecialreduction.FieldAppID).
//		Scan(ctx, &v)
//
func (usrq *UserSpecialReductionQuery) Select(fields ...string) *UserSpecialReductionSelect {
	usrq.fields = append(usrq.fields, fields...)
	selbuild := &UserSpecialReductionSelect{UserSpecialReductionQuery: usrq}
	selbuild.label = userspecialreduction.Label
	selbuild.flds, selbuild.scan = &usrq.fields, selbuild.Scan
	return selbuild
}

func (usrq *UserSpecialReductionQuery) prepareQuery(ctx context.Context) error {
	for _, f := range usrq.fields {
		if !userspecialreduction.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if usrq.path != nil {
		prev, err := usrq.path(ctx)
		if err != nil {
			return err
		}
		usrq.sql = prev
	}
	return nil
}

func (usrq *UserSpecialReductionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserSpecialReduction, error) {
	var (
		nodes = []*UserSpecialReduction{}
		_spec = usrq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*UserSpecialReduction).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &UserSpecialReduction{config: usrq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(usrq.modifiers) > 0 {
		_spec.Modifiers = usrq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, usrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (usrq *UserSpecialReductionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := usrq.querySpec()
	if len(usrq.modifiers) > 0 {
		_spec.Modifiers = usrq.modifiers
	}
	_spec.Node.Columns = usrq.fields
	if len(usrq.fields) > 0 {
		_spec.Unique = usrq.unique != nil && *usrq.unique
	}
	return sqlgraph.CountNodes(ctx, usrq.driver, _spec)
}

func (usrq *UserSpecialReductionQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := usrq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (usrq *UserSpecialReductionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   userspecialreduction.Table,
			Columns: userspecialreduction.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: userspecialreduction.FieldID,
			},
		},
		From:   usrq.sql,
		Unique: true,
	}
	if unique := usrq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := usrq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userspecialreduction.FieldID)
		for i := range fields {
			if fields[i] != userspecialreduction.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := usrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := usrq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := usrq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := usrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (usrq *UserSpecialReductionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(usrq.driver.Dialect())
	t1 := builder.Table(userspecialreduction.Table)
	columns := usrq.fields
	if len(columns) == 0 {
		columns = userspecialreduction.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if usrq.sql != nil {
		selector = usrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if usrq.unique != nil && *usrq.unique {
		selector.Distinct()
	}
	for _, m := range usrq.modifiers {
		m(selector)
	}
	for _, p := range usrq.predicates {
		p(selector)
	}
	for _, p := range usrq.order {
		p(selector)
	}
	if offset := usrq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := usrq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (usrq *UserSpecialReductionQuery) Modify(modifiers ...func(s *sql.Selector)) *UserSpecialReductionSelect {
	usrq.modifiers = append(usrq.modifiers, modifiers...)
	return usrq.Select()
}

// UserSpecialReductionGroupBy is the group-by builder for UserSpecialReduction entities.
type UserSpecialReductionGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (usrgb *UserSpecialReductionGroupBy) Aggregate(fns ...AggregateFunc) *UserSpecialReductionGroupBy {
	usrgb.fns = append(usrgb.fns, fns...)
	return usrgb
}

// Scan applies the group-by query and scans the result into the given value.
func (usrgb *UserSpecialReductionGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := usrgb.path(ctx)
	if err != nil {
		return err
	}
	usrgb.sql = query
	return usrgb.sqlScan(ctx, v)
}

func (usrgb *UserSpecialReductionGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range usrgb.fields {
		if !userspecialreduction.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := usrgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := usrgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (usrgb *UserSpecialReductionGroupBy) sqlQuery() *sql.Selector {
	selector := usrgb.sql.Select()
	aggregation := make([]string, 0, len(usrgb.fns))
	for _, fn := range usrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(usrgb.fields)+len(usrgb.fns))
		for _, f := range usrgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(usrgb.fields...)...)
}

// UserSpecialReductionSelect is the builder for selecting fields of UserSpecialReduction entities.
type UserSpecialReductionSelect struct {
	*UserSpecialReductionQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (usrs *UserSpecialReductionSelect) Scan(ctx context.Context, v interface{}) error {
	if err := usrs.prepareQuery(ctx); err != nil {
		return err
	}
	usrs.sql = usrs.UserSpecialReductionQuery.sqlQuery(ctx)
	return usrs.sqlScan(ctx, v)
}

func (usrs *UserSpecialReductionSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := usrs.sql.Query()
	if err := usrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (usrs *UserSpecialReductionSelect) Modify(modifiers ...func(s *sql.Selector)) *UserSpecialReductionSelect {
	usrs.modifiers = append(usrs.modifiers, modifiers...)
	return usrs
}
